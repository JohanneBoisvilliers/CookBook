<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="@dimen/radius_card_horizontal"
    app:cardElevation="3dp"
    android:layout_margin="@dimen/radius_card_horizontal">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/photo_container"
            android:layout_width="match_parent"
            android:layout_height="@dimen/base_height_card"
            android:background="@color/colorPrimary"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_below="@+id/photo_container"
            android:gravity="center_vertical"
            android:background="@color/colorPrimary"
            android:orientation="vertical">

        <TextView
            android:id="@+id/recipe_category"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Starter"
            android:maxLines="1"
            style="@style/TextAppearance.MaterialComponents.Overline"
            android:textSize="@dimen/text_bottom_overline"
            android:layout_marginStart="@dimen/margin_text_bottom"
            android:layout_marginEnd="@dimen/margin_text_bottom"
            android:textColor="@color/colorLight"
            android:fontFamily="@font/playfair_display"
            android:ellipsize="end" />
        <TextView
            android:id="@+id/recipe_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Recipes"
            android:maxLines="1"
            android:textSize="@dimen/text_bottom_gradient_size"
            android:layout_marginStart="@dimen/margin_text_bottom"
            android:layout_marginEnd="@dimen/margin_text_bottom"
            android:textColor="@color/colorAccent"
            android:fontFamily="@font/playfair_display"
            android:ellipsize="end" />
        </LinearLayout>
    </RelativeLayout>

</androidx.cardview.widget.CardView>
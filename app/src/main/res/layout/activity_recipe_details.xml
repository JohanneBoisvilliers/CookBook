<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".recipeDetailsPage.RecipeDetailsActivity">

    <androidx.viewpager2.widget.ViewPager2
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/viewPager_recipe_details"
        android:layout_width="match_parent"
        android:layout_height="@dimen/viewpager_height" />

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/rd_tab_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabBackground="@drawable/tab_selector"
        app:tabGravity="center"
        app:tabIndicatorHeight="0dp"
        app:layout_constraintBottom_toBottomOf="@id/viewPager_recipe_details"/>

    <TextView
        android:id="@+id/test_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ingredients"
        android:textStyle="bold"
        android:textSize="30sp"
        app:layout_constraintTop_toBottomOf="@id/viewPager_recipe_details"
        app:layout_constraintLeft_toLeftOf="parent"
        android:paddingStart="@dimen/margin_to_border"/>

</androidx.constraintlayout.widget.ConstraintLayout>